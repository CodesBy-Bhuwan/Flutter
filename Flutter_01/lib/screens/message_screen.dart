import 'package:flutter/material.dart';

// Simple Message Screen (UI only)
class MessageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Messages"),
      ),
      body: Center(
        child: Text("Chat UI coming soon..."),
      ),
    );
  }
}